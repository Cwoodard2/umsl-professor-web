import React from "react";
import StandardPage from "../components/StandardPage";

const Homepage = (props: any) => {
    return (
        <StandardPage>
            <h1 className="text-green-600">This is the start of something great!</h1>
        </StandardPage>
    );
};

export default Homepage;